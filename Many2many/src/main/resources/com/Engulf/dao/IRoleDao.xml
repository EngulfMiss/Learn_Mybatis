<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Engulf.dao.IRoleDao">
    <!-- 定义roleMap -->
    <resultMap id="roleMap" type="com.Engulf.domain.Role">
        <id property="roleId" column="rid"></id>
        <result property="roleName" column="role_name"></result>
        <result property="roleDesc" column="role_desc"></result>
        <collection property="users" ofType="com.Engulf.domain.User">
            <id property="id" column="id"></id>
            <result property="username" column="username"></result>
            <result property="address" column="address"></result>
            <result property="sex" column="sex"></result>
            <result property="birthday" column="birthday"></result>
        </collection>
    </resultMap>
    <!-- 查询所有 -->
    <!-- 由于多对多有第三张表，所以要进行两次左外连接 -->
    <select id="findAll" resultMap="roleMap">
        SELECT u.*,r.id AS rid,r.role_desc,r.role_name FROM role r LEFT OUTER JOIN user_role ur ON r.id = ur.rid LEFT OUTER JOIN USER u ON ur.uid = u.id;
    </select>
</mapper>